here is the panel data which created by merging the finance data and trading data.
In the file 'codes/main.py', running the function 'build_data(para_flag_path = xxx)'
can create the panel data 'basic_data.parquet' in this folder. 